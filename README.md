# Collaborative filtering with R

An example of collaborative filtering in R, used in product recommendation

unzip it, and don't forget to install required packages (recommenderlab and reshape2)
run the code and test with GetRecommendations


